
AM_CPPFLAGS =	-I$(top_srcdir) \
			$(GTK_CFLAGS) \
			$(XFCONF_CFLAGS) \
			$(XFCE4UI_CFLAGS) \
			-DPREFIX=\""${prefix}\"" \
			-DDATADIR=\""${pkgdatadir}"\" \
			-DSYSCONFDIR=\""${sysconfdir}/${PACKAGE}"\" \
			-DLIBDIR=\""${libdir}/${PACKAGE}"\"

LIBS =	$(GTK_LIBS) \
		$(XFCONF_LIBS)  \
		$(XFCE4UI_LIBS)

